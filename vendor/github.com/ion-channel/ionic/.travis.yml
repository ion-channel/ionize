language: go

go:
  - 1.9
  - master

install:
  - make travis_setup

script:
  - make fmt vet lint
  - go test -v $(go list ./... | grep -v /vendor/)
  - make coverage analyze

notifications:
  email: false
