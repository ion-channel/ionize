language: go

go:
  - 1.8
  - tip

install:
  - go get -u github.com/golang/lint/golint

script:
  - make fmt vet lint
  - go test -v $(go list ./... | grep -v /vendor/)

notifications:
  email: false
